name: make lint
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: my-step
        run: |
        cd $GITHUB_WORKSPACE
        npm ci
        make lint
